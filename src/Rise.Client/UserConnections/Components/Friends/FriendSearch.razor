<label for="friend-search" class="sr-only">Zoek vrienden</label>
<div class="mt-3 relative">
    <div class="pointer-events-none absolute inset-y-0 left-3 flex items-center">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-neutral-500" viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m21 21-4.35-4.35M11 19a8 8 0 1 1 0-16 8 8 0 0 1 0 16z"/>
        </svg>
    </div>
    <input id="friend-search" type="text" placeholder="Zoek vriend"
           class="w-full rounded-full bg-white pl-9 pr-4 py-3 text-sm placeholder-neutral-500 outline-none focus:ring-2 focus:ring-[#127646]"
           @bind="Value" @bind:event="oninput" />
</div>

@code {
    [Parameter] public string? Value { get; set; }
    [Parameter] public EventCallback<string?> ValueChanged { get; set; }
}